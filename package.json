{
  "name": "vulnerable-app",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "4.17.1",           // ⚠️ CVE Version (DoS)
    "lodash": "4.17.11",           // ⚠️ CVE (Prototype pollution)
    "mongoose": "5.2.0"            // ⚠️ CVE Version (injection)
  }
}
